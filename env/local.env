## Never check in local.env


# .env - Configuration for local development
# L: export $(cat .env | grep -v '^#' | xargs)
# Alternativ: nutze dein IDE-Feature oder direnv/local env loader

# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres
# Spring property acp.postgres-endpoint wird aus ACP_POSTGRES_ENDPOINT gelesen
ACP_POSTGRES_ENDPOINT=jdbc:postgresql://localhost:5432/postgres

# Optional: Standard Spring datasource URL fallback
# SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/postgres

# AWS / LocalStack (for S3, DynamoDB Tests)
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_REGION=us-east-1
# overwriting
ACP_S3_ENDPOINT=http://s3.localhost.localstack.cloud:4566
ACP_DYNAMODB_ENDPOINT=http://dynamodb.localhost.localstack.cloud:4566

# Liquibase (optional; main control is in application.yml)
# SPRING_LIQUIBASE_ENABLED=true

